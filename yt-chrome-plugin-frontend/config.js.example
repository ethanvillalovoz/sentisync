// config.js
const CONFIG = {
  API_KEY: "your-youtube-api-key-here", // YouTube Data API key
  // This is either your local host url that you use running the server locally, or the url from your EC2 server
  API_URL: "http://EC2-Public-IPv4-address:port/"  // or your local URL if in dev
};
